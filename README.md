# a quick and dirty tool i made for binary <-> decimal conversions

for binary -> decimal: prefix binary number with 'b' (i.e. b0100 -> d4)

for decimal -> binary: prefix binary number with 'd' (i.e. d5 -> b0101)

to exit: 'e'